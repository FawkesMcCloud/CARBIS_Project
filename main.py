from UI import UI
import logging

if __name__ == "__main__":
    #logging.basicConfig(filename='log.txt', filemode="w", encoding="utf-8", level=logging.DEBUG, format='%(asctime)s %(message)s')
    UI()
